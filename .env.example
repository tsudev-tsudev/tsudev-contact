# .env.example — mẫu biến môi trường cho tsudev-contact
# Copy thành .env khi cần (.env đã nằm trong .gitignore, TUYỆT ĐỐI không commit).
#
# Trạng thái 20/08/2026: ứng dụng là app desktop tkinter chạy hoàn toàn cục bộ,
# KHÔNG đọc biến môi trường nào và KHÔNG có secret/API key.
# File này giữ chỗ theo docs/PROJECT_STRUCTURE.md; khi thêm biến mới, khai báo tại đây
# kèm mô tả 1 dòng và giá trị mẫu (không phải giá trị thật).
